# V2.5 email delivery checkpoint

This is development evidence, not a new public release declaration.

- [x] Gmail connection test accepted by the provider and received by the owner.
- [x] API account/email/registration/invitation/identity/MFA gate: 72 tests pass.
- [x] Web email/settings/signup/admin gate: 34 tests pass.
- [x] TypeScript check, production build, Ruff and diff checks pass.
- [x] Focused independent security review: no critical/important blockers.
- [x] Fresh private pre-deployment backup created.
- [x] Private deployment, migration `0019_account_email`, healthy API/web and
  visible password-recovery page checked; existing owner session retained.
- [x] Owner received recovery email, submitted a new password and signed in.
- [ ] Separate live new-member signup verification acceptance remains to be recorded.
- [x] Google OAuth hostname/provider configuration and sign-in implementation;
  owner confirmed both explicit linking and subsequent Google sign-in.
- [x] Owner authorized V2.5 development-branch publication after private acceptance.
- [ ] New immutable release promotion remains separate.

Known non-blocking observations: existing upstream Python test-client warnings,
React Router future-flag test notices, and Vite's large-bundle advisory. The
email request throttle is best-effort under concurrency. Physical hardware,
Apple sign-in and V3 remain outside this bundle.

See [email setup](EMAIL-SETUP.md) for configuration, compatibility, key recovery,
delivery limitations and the boundary between email and Google sign-in.

Private rollout reused the existing dependency images, copied only changed
application files and new static assets, and used the existing rollback helper.
The one source difference from the public baseline was import ordering; no
unrelated application behavior was overwritten. No GitHub branch or tag was
changed during the private rollout. Gmail credentials were imported directly from protected server storage
into encrypted database settings without exposing them in the source bundle.

Google configuration is also encrypted and optional. The Admin form supports
each installation's own credentials; the Docker installer points owners to it.
See [Google setup](GOOGLE-SIGN-IN.md). Final focused Google/email API check:
17 passed. No provider secrets or private host configuration are published.
