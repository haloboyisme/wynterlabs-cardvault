# V2.5.0 release readiness

Release evidence for the immutable `v2.5.0` tag. Earlier test results below are
recorded acceptance evidence; only the clean installation and release checks
were repeated during final closeout.

- [x] Owner accepted password recovery, Google linking and Google sign-in.
- [x] Disposable-inbox signup received verification mail, blocked unverified
  login, accepted verification once, rejected replay, and allowed member login.
- [x] Published Git history rewritten with generic project attribution and
  historical private-network references removed. Current V2/V2.5 file trees
  were unchanged. Original history retained in a private backup.
- [x] Post-rewrite reachable-history scan: 41 commits and 635 unique file blobs;
  no remaining detected personal-name or private-network findings and no match
  for the configured Google client ID or secret.
- [x] Fresh encrypted private backup copied off-host; ciphertext/manifest
  checksums passed. Isolated database restore passed at migration 0020.
- [x] Actual standalone Docker upgrade from an existing 1.0.0 installation to
  the 2.5.0 candidate passed, preserving user and collection-row counts.
- [x] Fresh Ubuntu 26.04 amd64 container: bootstrap installed Docker/Compose,
  built API/web from source, migrated an empty database through 0020, and
  started all four services healthy. Trusted local HTTPS, initial owner setup,
  setup closure, owner MFA requirement, and disabled Google/email defaults passed.
- [x] Newest off-host backup decrypted with the existing owner-held age identity;
  the archive was readable. The check streamed plaintext without saving it.
- [x] Version and installer guard promoted to 2.5.0; README/install instructions
  point to `v2.5.0`. Release tag is created from this checked source revision.

The clean test used 2 vCPU, 4 GiB RAM, 512 MiB swap and a 24 GiB disposable disk
with an empty catalog; the documented 60 GiB minimum remains recommended for
actual collections, images and backups. It did not benchmark a full catalog.
The installer correctly rejected an unsupported Ubuntu version. One test-only
email fixture was corrected from a reserved `.invalid` domain rejected by the
validator to an example address; no application change was needed. Mail stayed
disabled and no message was sent from the clean test installation.

Known non-blocking build notices: dependency install-script notices and the
existing Vite large-chunk advisory. Production data was not changed by this check.

History rewriting changes commit IDs and affected V2 tag targets. Existing
clones should be freshly cloned or carefully reconciled; do not merge old
history back into cleaned branches. Old external clones and cached GitHub
objects are outside the reachable-history scan and may require separate removal.

Email test inbox provider: [Mail.tm](https://mail.tm/). No real collection data
was added to the disposable account. No provider credentials are in this report.
