import{render,screen,fireEvent}from"@testing-library/react";import{MemoryRouter}from"react-router-dom";import{it,expect,afterEach,vi}from"vitest";import{WorkspaceEffects,WorkspaceEffectsSettings}from"./WorkspaceEffects";import{EFFECTS_KEY,readEffects}from"../lib/workspace-effects";
afterEach(()=>{localStorage.removeItem(EFFECTS_KEY);vi.unstubAllGlobals();});
it("pauses background animation during scanning and cleans up",()=>{const{unmount}=render(<MemoryRouter initialEntries={["/scan"]}><WorkspaceEffects/></MemoryRouter>);expect(document.documentElement.dataset.effectsMotion).toBe("false");expect(document.documentElement.dataset.effectsPower).toBe("low");unmount();expect(document.documentElement.dataset.effectsMotion).toBeUndefined();});
it("respects reduced motion without a frame loop",()=>{vi.stubGlobal("matchMedia",()=>({matches:true,addEventListener:vi.fn(),removeEventListener:vi.fn()}));render(<MemoryRouter><WorkspaceEffects/></MemoryRouter>);expect(document.documentElement.dataset.effectsMotion).toBe("false");});
it("starts interface sounds off and saves an explicit choice",()=>{render(<WorkspaceEffectsSettings/>);expect(screen.getByLabelText("Click sounds")).not.toBeChecked();fireEvent.click(screen.getByLabelText("Click sounds"));expect(readEffects().clicks).toBe(true);});
